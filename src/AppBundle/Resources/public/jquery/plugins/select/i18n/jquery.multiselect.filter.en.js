/* English initialisation for the jQuery UI multiselect plugin. */

(function ( $ ) {

$.extend($.ech.multiselectfilter.prototype.options, {
	label: "Filter:",
	placeholder: "Enter keywords"
});

})( jQuery );
